---
permalink: /blog/bookmarks/feed.xml
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site.title }} — Bookmarks</title>
    <link href="{{ site.url }}/blog/bookmarks/feed.xml" rel="self"/>
    <link href="{{ site.url }}/blog/bookmarks/"/>
    <updated>{{ collections.bookmarks | getNewestCollectionItemDate | dateToRfc3339 }}</updated>
    <id>{{ site.url }}/blog/bookmarks/</id>
    <author><name>{{ site.author }}</name></author>
    {% for post in collections.bookmarks %}
    <entry>
        <title>{{ post.data.title }}</title>
        <link href="{{ site.url }}{{ post.url }}"/>
        <updated>{{ post.date | dateToRfc3339 }}</updated>
        <id>{{ site.url }}{{ post.url }}</id>
        <summary>{{ post.data.description }}</summary>
        <content type="html">{{ post.templateContent | htmlToAbsoluteUrls(site.url) | safe }}</content>
    </entry>
    {% endfor %}
</feed>
